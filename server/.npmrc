# npm 配置文件 - 解决阿里云服务器安装超时问题

# 使用淘宝镜像源
registry=https://registry.npmmirror.com

# 增加超时时间到5分钟
timeout=300000

# 设置最大重试次数
fetch-retries=5

# 设置重试间隔
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# 禁用进度条（在服务器环境中更稳定）
progress=false

# 设置日志级别
loglevel=warn